describe("Checkout page", () => {
  it("User can navigate to checkout", () => {});
  // homepage
  // search item
  // add item
  // checkout and pay
  it("User with failed card credentials see error", () => {});

  it("User cannot checkout with button disabled", () => {});

  it("User can return to basket to alter items", () => {});

  it("User can see total amount", () => {});

  it("User can see items they are buying", () => {});

  it("User can enter details and submit", () => {});

  it("User can go back and change quantity and checkout with new amount", () => {});

  it("User that isnt logged in cannot checkout", () => {});
});
